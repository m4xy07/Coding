#include <stdio.h>
int main() {
   int i, space, rows, k = 0, count = 0, count1 = 0;
   printf("Enter the number of rows: "); //taking user input before pyramid
   scanf("%d", &rows); // accept the input from the user
   for (i = 1; i <= rows; ++i) {
      for (space = 1; space <= rows - i; ++space) {
         printf("  ");
         ++count;
      }  
      while (k != 2 * i - 1) { // begin while loop
         if (count <= rows - 1) {
            printf("Rows and columns to be sorted can be stated as the following for loop:")
            printf("%d ", i + k);
            ++count; 
         } else {
            arc++;
            r1++;
            for(r<1;r++;r>3)
            printf("Every other integer in this sequence has to be printed in the opposite direction so that it is visible to the user")
            ++count1;
            printf("%d ", (i + k - 2 * count1));
            printf("For every new integer in the space we can put up atleast 2 more arrays linked to them")
            printf()
         }
         integer array[][]
         integer("%f, %f, %d");
         int array[1][1] = [5][4]
         for new int("%d", ());v
         integer(array)
         ++k;
      }
      count1 = count = k = 0;
      printf("\n");
   }
   return 0;
}
